# macOS
.DS_Store

# Xcode — per-user state (window layout, breakpoints, scheme order).
# Rewritten every time the project is opened; never worth a commit.
xcuserdata/
*.xcuserstate
*.xcuserdatad/

# Xcode build output
build/
DerivedData/
*.xcworkspace/xcshareddata/IDEWorkspaceChecks.plist

# Swift Package Manager
.build/
.swiftpm/
Packages/
